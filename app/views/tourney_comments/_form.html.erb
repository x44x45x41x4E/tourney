<%= form_for(@tourney_comment) do |f| %>
  <% if @tourney_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tourney_comment.errors.count, "error") %> prohibited this tourney_comment from being saved:</h2>

      <ul>
      <% @tourney_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
